# ===================================
# AWS RDS 프로덕션 환경 설정 템플릿
# ===================================
# 사용법:
# 1. 이 파일을 .env로 복사
# 2. 실제 값으로 변경
# 3. S3에 업로드: aws s3 cp .env s3://account-app-bucket-20260114/ --region ap-northeast-2
# ===================================

# RDS 데이터베이스 설정
DB_HOST=your-rds-endpoint.ap-northeast-2.rds.amazonaws.com
DB_PORT=3306
DB_NAME=account
DB_USER=admin
DB_PASSWORD=your-secure-password

# 개발 환경용 설정 (docker-compose.yml용)
DOC_DB_HOST=db
DOC_DB_NAME=account
MYSQL_DATABASE=account
MYSQL_ROOT_PASSWORD=root-password
MYSQL_USER=account_user
MYSQL_PASSWORD=account_password
